# Courses

## Endpoints

- `GET /healthcheck`
- `GET /v1/courses`

## Getting Started

With Docker

```shell
docker build -t academy-courses .
docker run -p 3001:80 -it --rm --name academy-courses academy-courses
```
